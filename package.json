{
  "name": "arcant-master",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@react-three/fiber": "^9.6.1",
    "@vercel/speed-insights": "^2.0.0",
    "three": "^0.185.1"
  }
}
